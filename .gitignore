# Build / IDE
.gradle/
.idea/
*.iml
local.properties
build/
app/build/
captures/
.externalNativeBuild/
.cxx/

# Never commit signing material
*.jks
*.keystore
keystore.properties
google-services.json

# Never commit build binaries — the app is distributed via the stores (Play / App Store), not the repo
*.apk
*.aab

# OS noise
.DS_Store
Thumbs.db

# app guide
APP_GUIDE.md

# Claude Code local machine state (per-machine; CLAUDE.md is the shared source of truth)
.claude/settings.local.json

# Scratch: pasted session transcripts and working notes, not source (despite the .sh names).
# claude.sh was tracked by mistake through 2.5.0 and was untracked in 2.6.0; the file itself is
# left on disk, only its history stops here.
claude.sh
plan.md
session.sh

# Console/device screenshots pulled in for a session
prod_gold.png

# Generated: the app's own CSV export, pulled off a device. Regenerate from the app, not the repo.
gold_index_history.csv

# research data caches are reproducible (see research/README.md)
research/cache/
__pycache__/
